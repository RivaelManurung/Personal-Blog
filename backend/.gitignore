# Environment
.env

# Build output (compiled binaries at the package root — anchored with a
# leading slash so they do not also match the cmd/server source directory).
/bin/
/server
/migrate
/seed

# Uploaded media (local storage driver)
/storage/uploads/

# Go
*.test
*.out
